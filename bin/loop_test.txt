# This file tests while loops and boolean expressions

# Variable definitions
var offset = true;
var sentence = "This is a quality sentence.";
var some_constant = offset;
var a_number = 9;
var variable = offset + some_constant;
var integer = a_number + "a string";
var b = false;
var i = "the next statement is true.";
var statement = "i is false.";
var xs = "yes";

# Simple while loop with constant boolean expression
while true do
  print("Infinite loop.");
end

# No statements
while false do
end

# Simple boolean expressions
while variable >= 4 do
  variable = variable - 1;
end

while some_constant == integer do
  variable = some_constant;
  integer = readint("Throw some numbers this way.");
end

# Negation
while not b do
end

# Complex boolean expressions
while integer < variable or a_number <= some_constant do
  print("The expression is true.");
end

while 4 < 5 or true do
  var num = readint("message");
  var c = readstr("number");
end

# Nested loops
while i < 5 do
  var ys = "this" + "that";
	xs[i] = ys[i];
	i = i + 1;
	while statement > sentence do
		while not b do
			b = true;
		end
		statement = sentence + offset;
	end
end
