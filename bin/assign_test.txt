# Testing assignment statements and all sorts of right hand side expressions

var number = 4;
var large_number = 32565;

# Mathematical equations
var integer = number + large_number;
var num = number / large_number - integer;
var complex = integer + number / num * large_number;

# Constant boolean expressions
var bool = true;
var bool2 = false;

# String assignments
var message = "This message will explode in 3...2...";

# List assignment
# But first, some variable definitions
var this = number;
var t = large_number;
var a = t - large_number;
var hat = num;
var some_value = complex * integer;
var element = hat + t;
var orange = element;
var last = true;
var outdex = 13;
var[] index;
var[] illegal_but_accepted;
var[] uninitialized;

var list = [1,2,3,5];
index[this - t] = outdex[t + hat] * some_value;
illegal_but_accepted[orange] = [a, 12, "four", true] * [element, last];
