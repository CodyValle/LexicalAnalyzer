# Testing assignment statements and all sorts of right hand side expressions

int number = 4;
int large_number = 32565;

# Mathematical equations
int integer = number + large_number;
float num = number / large_number - integer;
int complex = integer + number / num * large_number;

# Constant boolean expressions
boolean bool = true;
boolean bool2 = false;

# String assignments
string message = "This message will explode in 3...2...";

# List assignment
# But first, some variable definitions
int this = number;
int t = large_number;
int a = t - large_number;
float hat = num;
string some_value = complex * integer;
boolean element = hat + t;
string orange = element;
boolean last = true;
int outdex = 13;
int[] index;
boolean[] illegal_but_accepted;
string[] uninitialized;

int list = [1,2,3,5];
index[this - t] = outdex[t + hat] * some_value;
illegal_but_accepted[orange] = [a, 12, "four", true] * [element, last];
